<h2>Song Form</h2>

<%= form_for @song do |f| %>
<%= f.label :name %><br>
<%= f.text_field :name %><br>

<%= f.label :artist_id %>
<%= f.text_area :artist_id %>

<%= f.label :genre_id %>
<%= f.text_area :genre_id %>

<%= f.submit %>

<% end  %>
